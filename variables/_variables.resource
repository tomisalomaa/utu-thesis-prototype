# Here are variables shared by multiple tests
*** Variables ***
# Constant (immutable, don't change during execution) variables in CAPS
${BROWSER}    Chrome
${ENVIRONMENT}        local

# Web element locators are usually written in CAPS
${LOC_LOGIN_BUTTON}    id:login-button
